# MAC-9 Step 2 — WiGLE module dependencies.
#
# Pinned per CEO ratification of Q1 (state derivation methodology) at
# comment ea4db5e2 on MAC-9 (2026-05-04T18:34Z): option (b) `reverse_geocoder`
# PyPI is the minimum-viable extra dep for DeFlock lat/lon -> admin1
# (US state) lookups, used in `db/sources/wigle.py` to populate
# `wigle_anchor_priority.derivation_method='deflock_reverse_geocode'`.
#
# Install:  pip install -r requirements-wigle.txt
#
# Bible §1 stdlib-first preference allows deps "when stdlib genuinely
# doesn't suffice" (Q1 ratification: bbox+centroid produces 24.6%
# multi-match prevalence; canary Amarillo TX -> OK at lat=35.222,
# lon=-101.8313 mis-assigns; reverse_geocoder correctly returns
# admin1='Texas').
#
# Transitive: numpy, scipy (data-science loader bringing in city DB).

reverse_geocoder==1.5.1
