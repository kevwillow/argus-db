# Argus — Project State

**Current phase:** 2 — Tier 1 Structured Sources (**all 4/4 done**: IEEE OUI ✓, Wireshark `manuf` ✓, EFF Atlas ✓, **DeFlock ✓**; [MAC-6](/MAC/issues/MAC-6) `in_review` to CEO 2026-05-04T~05:5xZ awaiting ratification before Checkpoint 2 dispatch to user)
**Last checkpoint passed:** 1 (signed off 2026-05-04T03:23:31Z by user comment [d08ee4a8](/MAC/issues/MAC-1#comment-d08ee4a8-e525-4c19-9886-89f0f95bf1c3))
**Next checkpoint:** 2 — Tier 1 structured-source staging populated (all four sources). Spot-check 20 random records. User approval to proceed.
**Status:** SourceWorker MAC-6 DeFlock ingest **landed**. `db/sources/deflock.py` authored mirroring `eff_atlas.py` shape (pure stdlib, delete-by-`source_id` idempotency, single retry on URLError). Ingested under `--raw-subdir 20260504T052752Z` (no re-fetch). `sources.id=6` (`crowdsourced`, tier=1, `last_status=ok`); `extraction_runs.id=11` initial + `.id=12` idempotency re-run, both `records_in=records_out=101,597`, `status=ok`. `deployment_observations` for `source_id=6`: 101,597 rows. **Total Phase 2 staging: 109,736 raw_observations + 116,668 deployment_observations (Atlas 15,071 + DeFlock 101,597); identifiers=0; conflicts=0; schema_version=2 (no new migration).** §11 #3 PII redaction (person regex only per CEO decision 5): 22 hits across 22 nodes — all agency-name FPs (`Trooper`-token in agency names like "Orange County [...] Trooper"); raw on-disk un-redacted person-pattern matches still total 22 (audit trail intact per §7.2). Plate regex deliberately skipped per decision 5; Phase-5 reconsider trigger logged in `extraction_runs.notes`. Word-boundary vendor sweep reproduces the discovery numbers exactly: `\bFlock Safety\b` mfg=73,459 / brand=4,793 / op=2,232; `\bMotorola Solutions\b` mfg=4,063 / brand=283; Genetec mfg=1,897; **Cradlepoint/Sierra Wireless/Cellebrite/Getac/BRINC/Skydio/WatchGuard/L3Harris all 0** (cop-car cluster sub-system half remains structurally invisible to ALPR-mapping projects — Phase 3 FCC-ID is the right path). Cross-source Atlas∩DeFlock operator overlap (case-insensitive exact match): **574 distinct agencies** match; top: San Diego PD 497 DeFlock cameras + 1 Atlas deployment, Detroit PD 440+1, SF PD 323+1, Chicago PD 206+1, NYPD 204+1 — high-value Phase-3 join breadcrumbs (single Atlas journalism reference → N geo-anchored cameras). 25/25 dedup tests pass. ODbL attribution captured verbatim in `sources.notes.license_attribution`. Committed as `0c8da7d feat(ingest): DeFlock — Phase 2 source 4/4 staged via Path B (CDN)`. MAC-6 `in_review` to CEO with full deliverable comment pending. MAC-1 stays `in_review` with user as standing kickoff thread.

## Active sub-agents
- **DBArchitect** (`6c93a466-d498-49e0-b7af-3fc0d08eb2b0`) — Phase 1 complete. Off-rotation until Phase 5 export design (or sooner if §4.1 enum extension for `in_vehicle_router` becomes blocking).
- **SourceWorker** (`9cf8ff12-53c3-4f83-837f-3142d8d1d151`) — MAC-3 ✓; MAC-4 ✓; MAC-5 ✓; [MAC-6](/MAC/issues/MAC-6) ingest landed 2026-05-04T~05:5xZ (101,597 rows, idempotency re-run identical, 22 PII redactions all FPs, vendor sweep + Atlas overlap reproduced); reassigning `in_review` to CEO with deliverable. Sequential single-worker dispatch through Tier 1 complete.

## Last action
SourceWorker heartbeat 2026-05-04T~05:5xZ — MAC-6 DeFlock ingest landed; **Phase 2 staging complete (4/4)**:
1. **Module authored.** `db/sources/deflock.py` (~830 LoC, pure stdlib `urllib`/`json`/`sqlite3`/`re`) mirrors `eff_atlas.py` shape — `--raw-subdir` reuse, single retry on `URLError`, delete-by-`source_id` then bulk-insert, deterministic insert order via `sort(key=osm_id)`. All 7 CEO ratification decisions encoded in module docstring + `sources.notes` JSON + `extraction_runs.notes`. No new migration; reuses `deployment_observations` (CP4 / commit `d81de3b`).
2. **Counts.** `sources.id=6` (`name='DeFlock (OSM-mirrored ALPR camera locations via cdn.deflock.me)'`, `source_type='crowdsourced'`, `tier=1`, `last_status='ok'`); `extraction_runs.id=11` initial + `.id=12` idempotency re-run, both `records_in=records_out=101,597`, `status='ok'`. `deployment_observations` for `source_id=6`: 101,597 rows. Total Phase 2 staging: **116,668 deployment_observations** (Atlas 15,071 + DeFlock 101,597) + 109,736 raw_observations from MAC-3/4. `identifiers=0`, `conflicts=0`, `schema_version=2`.
3. **§11 #3 PII redaction (person regex only per decision 5).** 22 hits across 22 nodes — all agency-name false positives (e.g. "Orange County [...] Trooper", "Killingworth Resident State Trooper", "Mendocino County Trooper"). Zero real officer-name PII. Raw on-disk audit trail intact: 22 un-redacted person-pattern matches in `raw/deflock/20260504T052752Z/regions/*.json` (full payloads preserved verbatim per §7.2). Plate regex deliberately skipped per decision 5 — CDN whitelist excludes freetext fields, direction-string FPs would scramble structured data with 0 PII benefit. Phase-5 reconsider trigger logged in `extraction_runs.notes`: "if upstream `alpr_cache.py` whitelist changes (e.g. `description`/`note` keys propagated), revalidate plate regex".
4. **Word-boundary vendor sweep — full canonical labels (per MAC-4/MAC-5 lesson).** Reproduces CEO ratification numbers exactly:
   - **Flock Safety**: mfg=73,459 / brand=4,793 / op=2,232 (largest concentration of any Phase 2 source)
   - **Motorola Solutions**: mfg=4,063 / brand=283 / op=0
   - **Genetec**: mfg=1,897 / brand=129 / op=0
   - **Leonardo**: mfg=746 / brand=1 (Italian ALPR vendor; new label vs Atlas)
   - **Axis Communications**: mfg=458 / brand=1
   - **Ubicquia**: mfg=442 / brand=0 (smart-streetlight ALPR retrofit; new label vs Atlas)
   - **Rekor**: mfg=386 / brand=13 / op=8
   - **Ekin**: mfg=372 / brand=27 (separate from "Ekin Box Spotter" 323 — Phase-5 canonicalization)
   - **Neology**: mfg=370 / brand=44 (variants `Neology, Inc.` 401 / `Neology` 370 — Phase 5)
   - **Hikvision**: mfg=251; **Dahua**: mfg=120; **Verkada**: mfg=99; **Avigilon**: mfg=72/brand=37
   - **Axon Enterprise**: mfg=62 / op=1 (Mineola PD); **Vigilant**: mfg=16 / op=1
   - **Cradlepoint, Sierra Wireless, Cellebrite, Getac, BRINC, Skydio, WatchGuard, L3Harris**: all 0 — confirms cop-car cluster sub-system half is structurally invisible to ALPR-mapping projects. Phase 3 FCC-ID + SAM.gov remains the right path.
5. **Cross-source Atlas∩DeFlock operator overlap (case-insensitive exact match).** **574 distinct agencies** match between DeFlock's 1,832 distinct operators and Atlas's 7,747 distinct agencies. Top by DeFlock node count (Atlas deployment count for same name typically = 1, the geo-anchoring multiplier is the value-add): San Diego PD 497, Detroit PD 440, SF PD 323, Chicago PD 206, NYPD 204, Norfolk PD 164, LV Metro 155, Irvine PD 137, Albuquerque PD 123, Hampton PD 111. Cross-vendor join opportunity: a single Atlas journalism reference for "San Diego Police Department" + 497 DeFlock-mapped cameras = 497 geo-anchored deployment records derived from one Atlas record. **Surfaced for Phase 3+ inference; no promotion (§11 #8).**
6. **20-random-row spot-check.** Healthy: lat/lon coords plausibly land on US/EU streets, manufacturer/operator preserve raw verbatim (`Flock Safety`, `Neology, Inc.`, `Bosch Security Systems`, `Dahua`, `Nationale Politie`, `Mohegan Tribal Police Department`). One row with un-set tags but valid lat/lon (OSM `node` exists pre-tag-population) — preserved as-is per §7.2.
7. **Idempotency verified.** Two consecutive `--raw-subdir 20260504T052752Z` runs both produced 101,597 rows (`extraction_runs` 11 + 12 identical counts, `status='ok'`). The `UNIQUE(source_id, source_row_key)` backstop never fires under the delete-by-source-id pattern; structural defence not runtime path.
8. **No promotion to `identifiers`.** Phase 2 staging only per §11 #8. `identifiers` table remains 0 rows.
9. **25/25 dedup tests pass.** No regressions.
10. **Forward observations (post-ingest, not blocking).**
    - Vendor canonicalization queue (Phase 5): `Flock Safety` 73,459 / `Flock Safety Inc.` ~/`Flock Group Inc.` 490 / `Flock` (bare) — total raw distinct Flock-bearing strings: ~5; `Neology, Inc.` 401 / `Neology` 370; `Ekin` 372 / `Ekin Box Spotter` 323; `Motorola Solutions` 4,063 / typos `Mortorola Solutions` if present.
    - **New labels not in Atlas**: Leonardo 746 (Italian defense / ALPR), Ubicquia 442 (smart-streetlight retrofit), Bosch Security Systems (single-digit). Phase 5 cross-source join will surface vendors structurally absent from journalism-style sources.
    - Non-US footprint substantial: top non-US operators include `Nationale Politie` (Netherlands, 1,126), `Rigspolitiet` (Denmark, 292), Australian Neology installations.
    - Private-sector operators surface clearly: **Lowe's 1,188, The Home Depot 753** — retail-loss-prevention ALPR is a Phase 5 categorization the Bible's §2.1 doesn't currently anticipate. Surfacing for CEO/user judgment, not bible amendment territory.
11. **Committed as** `0c8da7d feat(ingest): DeFlock — Phase 2 source 4/4 staged via Path B (CDN)` (1 file, 833 insertions). Reassigning [MAC-6](/MAC/issues/MAC-6) `in_review` to CEO with this summary. **Phase 2 staging is complete.** Next: CEO assembles Checkpoint 2 to user (full Phase 2 vendor/tech/state/agency rollup across IEEE+Wireshark+Atlas+DeFlock + cop-car cluster heatmap).

CEO heartbeat 2026-05-04T~05:38Z — MAC-6 ratified, greenlight to ingest:
1. **Independent verification reproduced load-bearing worker claims.** Raw sha256s match manifest (`regions-index.json` `5d354ce6…0dc73`, `deflock.org-robots.txt` `dad07662…7d13a`); robots.txt confirms `Disallow: /api/` for `User-agent: *` (also a Cloudflare-managed AI-bot block targeting named crawlers like ClaudeBot/GPTBot — separate concern, doesn't apply since we're a generic stdlib HTTP client hitting `cdn.deflock.me`); 50 region tiles on disk; 101,597 total nodes / 0 OSM-id collisions; word-boundary vendor sweep reproduces exactly (`\bFlock Safety\b` mfg=73,459 / brand=4,793; `\bMotorola Solutions\b` mfg=4,063 / brand=283 / op=0; Genetec mfg=1,897; Cradlepoint/Sierra/Cellebrite/Getac/BRINC/Skydio/WatchGuard/L3Harris all 0); 23,234 operator-tagged nodes; person regex 36 vs worker's 22 hits — methodology variance, ALL 36 are agency-name FPs (`Marshall Police Department`, `Caldwell County Constable Pct 2`, `San Diego Sheriff Department`, etc.), zero real officer-name PII.
2. **All 7 ratification items confirmed.** (1) Path B (CDN) — DeFlock-hosted endpoint, honors `/api/` disallow + whitelist-as-PII-mitigation + preserves DeFlock attribution; (2) `source_type='crowdsourced'` per MAC-4/MAC-5 precedent + Phase 5 reconsider marker; (3) `source_url=https://cdn.deflock.me/regions/index.json` (per-tile URL captured in `notes.tile_url`); (4) `source_row_key=str(osm_id)` (0 collisions on snapshot, OSM IDs globally unique by spec); (5) apply person regex recall-first per CP4; **skip plate regex on Path B** because whitelist structurally excludes plate-bearing freetext fields (3,731 hits all FPs against `direction` compass-bearing strings + operator acronyms; applying would scramble structured data with 0 true positives) — tightening: log Phase-5 reconsider line "if upstream `alpr_cache.py` whitelist changes, revalidate plate regex"; (6) `technology_category="ALPR"` constant — Overpass filter `surveillance:type=ALPR` is the source-construction predicate, defensible to encode (not §11 #1 fabrication); (7) ODbL attribution captured verbatim in `sources.notes.license` + `license_attribution` — Export Worker dispatch must carry "© OpenStreetMap contributors. ODbL 1.0." downstream.
3. **No Bible amendment, no SAR, no new migration.** OSM-mirror is sub-source nuance, not a §4 schema or §4.1 enum issue. `deployment_observations` (CP4 commit `d81de3b`) accommodates DeFlock cleanly. `schema_version` stays at `2`. Surfacing OSM/ODbL identity in Checkpoint 2 brief, not codifying.
4. **MAC-6 PATCHed to SourceWorker `in_progress`** 2026-05-04T~05:38Z with full ratification comment quoting verification numbers + 7 decisions + forward observations + bible HEAD `d81de3b` + `--raw-subdir 20260504T052752Z` reuse instruction (no re-fetch). Reporting checklist from §10 of worker's proposal stands.
5. **Sequence:** SourceWorker authors `db/sources/deflock.py` mirroring `eff_atlas.py` shape (pure stdlib, delete-by-source_id idempotency, idempotency re-run for verification), authors zero new migrations, ingests under existing snapshot, reports back `in_review` with: row count (target ~101,597), word-boundary vendor sweep with full canonical labels, cross-source Atlas∩DeFlock operator overlap, person redaction hits, ODbL license string preserved in `sources.notes`. Then CEO assembles Checkpoint 2 to user (full Phase 2 vendor/tech/state/agency rollup across IEEE+Wireshark+Atlas+DeFlock + cop-car cluster heatmap).

SourceWorker heartbeat 2026-05-04T~05:33Z — MAC-6 DeFlock discovery pre-stage + ratification proposal posted; **stop-line hit** (OSM-mirrored / ODbL); awaiting CEO ratification on 7 decisions:
1. **Source identity discovered.** DeFlock is a thin Vue frontend (MIT, [github.com/FoggedLens/deflock](https://github.com/FoggedLens/deflock)) over OpenStreetMap. Reporting writes to OSM via iD editor or the DeFlock app; reading goes via `serverless/alpr_cache/src/alpr_cache.py` lambda which queries Overpass `node["man_made"="surveillance"]["surveillance:type"="ALPR"]; out body;` hourly and reprojects to `cdn.deflock.me/regions/{lat}/{lon}.json` with a 9-key tag whitelist (operator, manufacturer, brand, direction, camera:direction, surveillance:{brand,operator,manufacturer}, wikimedia_commons). Underlying data = OSM, license = **ODbL** (Open Database License 1.0) with attribution + share-alike requirements. DeFlock's MIT covers frontend code only.
2. **§11 #6 robots.txt finding.** `https://deflock.org/robots.txt` ends with `User-agent: * / Allow: / / Disallow: /api/`. The site's own data API is disallowed for all crawlers. `cdn.deflock.me` is a separate Cloudflare R2/S3 host (DeFlock's own published frontend uses it); no robots restriction applies. Three viable paths surfaced for ratification: (A) ❌ `deflock.org/api/` blocked, (B) ✅ `cdn.deflock.me` (whitelist-mitigated PII surface, DeFlock-attributed, hourly refresh) — recommended, (C) `overpass-api.de` direct (broader tag surface but PII risk on freetext fields, attributes to OSM not DeFlock).
3. **Pre-staged raw artifacts** under `raw/deflock/20260504T052752Z/` — 50 region tiles (`regions/{lat}_{lon}.json`, total 12,876,373 B), `regions-index.json` (637 B sha256 `5d354ce664a5…`), `deflock.org-{robots.txt,about.html,terms.html,privacy.html}` context, `fetch.py` reproducible discovery script, `manifest.json` with per-file URL/byte_count/sha256/http_status + license_note + license_attribution + robots_check + discovery_method. All HTTP 200. 0.25 s spacing on cdn fetches.
4. **Counts.** 101,597 unique OSM nodes (0 ID collisions across 50 tiles). Whitelist confirmed exactly: keys present == WHITELISTED_TAGS. ~93,542 (92.1%) in rough US lat/lon box. Lat range [-51.6, 69.7], lon range [-159.6, 176.2]. `source_row_key` = OSM `id` (string-cast) — globally unique by OSM construction.
5. **Word-boundary canonical vendor sweep** (per MAC-4/MAC-5 lessons; full canonical labels only): Flock Safety 73,459 mfg + 4,793 brand + 2,232 operator (largest concentration of any Phase 2 source); Motorola Solutions 4,063 mfg + 283 brand; Genetec 1,897 mfg + 129 brand; Axis Communications 458 mfg; Rekor 386 mfg + 13 brand; Ekin 372 mfg; Neology 370 mfg; Hikvision 251 mfg; Dahua 120; Verkada 99; Avigilon 72 mfg + 37 brand; Axon Enterprise 62 mfg + 1 operator (Mineola PD); Vigilant 16 mfg. **Cradlepoint, Sierra Wireless, Cellebrite, Getac, BRINC, Skydio, WatchGuard, L3Harris all 0** — confirms the standing advisory "ALPR-mapping projects are structurally invisible to in-vehicle subsystem vendors; route to Phase 3 FCC-ID." Brand variants flagged for Phase 5: Flock Safety / Flock / Flock Safety Inc / Flock Group Inc.; Motorola Solutions / Mortorola Solutions (typo) / Motorola; Neology, Inc. / Neology.
6. **PII assessment.** Person regex (MAC-5 pattern + token list) ran on all whitelisted-tag values: **22 hits, all 22 are agency-name FPs** ("Sheriff Department", "Constable Pct 2"). Plate regex: **3,731 hits, all FPs** — direction-string compass ranges (`"165-235"`) and operator acronyms (`"MPCALA"` from `"MPCALA Holdings, Inc."`). Recommended: apply person regex (recall over precision per CP4); skip plate regex on Path B because (a) zero real plates anywhere, (b) direction-field corruption with no PII benefit. CDN whitelist is **structurally safer** than Overpass on the PII axis.
7. **Schema fit.** Reuses `deployment_observations` cleanly. No migration. Projected post-ingest count: source_id=6 → 101,597 rows (16,668 total in table after Atlas + DeFlock). `source_url` = dataset URL pattern (`https://cdn.deflock.me/regions/index.json`); `lat`/`lon` populated (the value-add); `agency_name` = `tags.operator`; `vendor_raw` = `tags.manufacturer` || `tags.brand`; `technology_category="ALPR"` constant (entire dataset is ALPR by query construction); `notes` JSON catches all 9 whitelisted tags + tile path; `citation_url` = NULL (no per-row external citation in CDN payload). Identifier columns absent (§11 #1); no promotion (§11 #8).
8. **Cross-source overlap with Atlas (preview, not promoted).** Operator-string canonical match between DeFlock's 23,234 operator-tagged nodes and Atlas's `Agency` column will be a high-value Phase-3 join (e.g., San Diego PD: 497 DeFlock cameras + 1 Atlas deployment = geo-anchored 497 mapped cameras for that single Atlas deployment).
9. **Posted proposal comment** [4a1edbf9](/MAC/issues/MAC-6#comment-4a1edbf9) (18,296 chars). Reassigned [MAC-6](/MAC/issues/MAC-6) `in_review` to CEO. **No code written, no migration written, no DB rows written.** Awaiting ratification on the 7 decisions in §10 of the comment. After ratification → author `db/sources/deflock.py` mirroring `eff_atlas.py` shape (pure stdlib, `--raw-subdir 20260504T052752Z` reuse, delete-by-source_id idempotency, idempotency re-run for verification).

CEO heartbeat 2026-05-04T~05:2xZ — MAC-5 verified, closed `done`; MAC-6 DeFlock dispatched:
1. **Independent verification reproduced every SourceWorker quantitative claim.** Direct sqlite3 read of `db/argus.db`: schema_version=2 ✓; sources count=5 with id=5 EFF Atlas (`source_type='crowdsourced'`, tier=1, last_status='ok', sha256+byte_count match manifest) ✓; extraction_runs count=10 with ids 9+10 source_id=5, status=ok, records_in=records_out=15,071, deferred-decision marker preserved ✓; deployment_observations=15,071 all source_id=5 ✓; raw_observations=109,736 unchanged (39,355+6,380+6,992+57,009) ✓; identifiers=0 ✓; conflicts=0 ✓. PRAGMA table_info confirmed no mac/oui/ssid/uuid/candidate_identifier columns on `deployment_observations` (§11 #1) ✓. AOS001160 redacted form reads `"…according to [REDACTED-PERSON]."` ✓; DB-wide `LIKE '%Gary Smith%'` = 0; `LIKE '%[REDACTED-PERSON]%'` = 16 (matches the 16 AOSNUMBERs in `extraction_runs.notes`). Raw on-disk: sha256 `45fc50f04e005e8ee1fb5c3500060d247221dfff82bea37f7e7e8fc4fa365933` ✓; byte_count 8,361,059 ✓; `grep -c "Mayor Gary Smith"` on raw still 1 (§7.2 audit trail intact). 20-row spot-check rows verified verbatim (AOS000779 Azusa PD CA Flock ALPR + MuckRock URL; AOS004049 Charlotte-Mecklenburg PD NC Harris Corp. Cell-site Simulator + Vocativ archive; AOS002361 Collier County FL Motorola Solutions Camera Registry + CityProtect URL).
2. **Word-boundary vendor sweep reproduced exactly.** Flock Safety total/single/multi = 2,743/2,707/36 ✓; Flock × Gunshot = 45 ✓; Top Flock states TX 229 / IL 228 / OH 222 / IN 212 / CA 187 ✓; Axon 912/893/19 ✓; WatchGuard 181 ✓; Getac 17 ✓; SoundThinking exact 796 ✓; Cradlepoint / Sierra Wireless / Cellebrite / Hak5 / Magnet / Berla / Dedrone all 0 each ✓; canonicalization variants confirmed (WatchGuard 167 / Watchguard 14, SoundThinking 797 / Soundthinking 2, ShotSpotter 125 / Shotspotter 11, BRINC 3 / Brinc 20).
3. **One label-vs-pattern precision drift caught.** Worker wrote "Motorola Solutions: 301 mentions (217 single + 84 multi)." Reality: `\bMotorola Solutions\b`-strict = 237 (217 exact + 20 multi-vendor co-occurrences); `\bMotorola\b`-loose = 301 (217 exact "Motorola Solutions" + 59 bare "Motorola" + 2 typos `Soluitions`/`Solutins` + 23 other Motorola-containing values incl. `Motorola WatchGuard`, `Fusus, Motorola`, `Motorola, Wolfcom`, multi-vendor sets). 217+84=301 is arithmetically consistent if "84 multi" = "all-non-exact-Motorola" but the natural read of "multi-vendor co-occurrences with Motorola Solutions" is 20. **Not a fabrication** (every row in 301 contains "Motorola"; canonical-name divergence is real and the worker correctly flagged it for Phase 5). It is a label-vs-pattern precision drift, sibling to MAC-4 Axon `13` substring noise (different mechanism: pattern broader than label, vs MAC-4: substring noise within a single name). Closed without changes-requested; codified the lesson into MAC-6 dispatch.
4. **MAC-5 PATCHed to `done`** with full verification table + Motorola precision flag + bible-compliance summary + Phase 2 status table (3/4 Tier 1 sources landed). Comment posted on MAC-5 thread.
5. **MAC-6 DeFlock dispatched** as new issue ([MAC-6](/MAC/issues/MAC-6), id `f83a5921-f7d5-4753-a4ec-990342e065fc`, parentId=MAC-1, goalId=`374f4ac8-…`, projectId=`7e80ffb6-…`, assigneeAgentId=SourceWorker, status=todo, priority=medium). Description spells out:
   - **Reuse `deployment_observations` schema (no new migration)** — bible §4.2 explicitly names DeFlock at CP4 ratification.
   - **`source_type='crowdsourced'` default with OSM/ODbL stop-line** — if DeFlock turns out to be OSM/Overpass-mirrored under ODbL, surface it (don't extend §4.1 enum unilaterally).
   - **Two-step discovery + ratification flow mirroring MAC-5** — pre-stage raw + manifest + license + robots.txt check + source_row_key stability evidence; post ratification proposal; reassign CEO `in_review`; CEO ratifies; ingest. Single-delivery collapse only allowed if all discovery checks come back clean and zero stop-line concerns.
   - **§11 #3 PII redaction = person + plate.** Same person regex shape as MAC-5 (rank-token + name); add plate regex `\b[A-Z0-9]{2,4}[-\s]?[A-Z0-9]{3,4}\b` collapsing to `[REDACTED-PLATE]` (recall-first; Phase 5 stricter pass).
   - **Word-boundary vendor sweep with FULL canonical label** (e.g., `\bMotorola Solutions\b` not `\bMotorola\b`); broader-pattern counts must be reported under a clearly distinct label.
   - **Geo (`lat`/`lon`) is the value-add** of DeFlock vs Atlas — populate directly. Reverse-geocoding out of scope (§7.2).
   - **Cross-source overlap with Atlas** (which deployments appear in both) is the most interesting derived signal — surface but no promotion (§11 #8).
6. **Sequence reaffirmed:** MAC-6 → Checkpoint 2 to user (full Phase 2 vendor/tech/state/agency rollup across IEEE+Wireshark+Atlas+DeFlock + cop-car cluster heatmap).
7. **User autonomous-run authorization** ([a11e2fa1](/MAC/issues/MAC-1#comment-a11e2fa1)) carved out: bible §4 schema *extensions* (new enum / new tables, not applications) still wake the user; worker stop-lines I can't resolve from rules; §11 hard-rule triggers; Checkpoint 2 itself. MAC-5 close + MAC-6 dispatch are routine within that scope. MAC-1 stays `in_review` with user.

SourceWorker heartbeat 2026-05-04T~05:1xZ — MAC-5 EFF Atlas ingest landed:
1. **Migration authored.** `db/migrations/0002_deployment_observations.sql` matches bible-at-HEAD §4.2 (CP4 commit `d81de3b`) verbatim — `(source_id, source_row_key)` UNIQUE backstop for idempotency, no CHECK on `technology_category` / `vendor_raw` (Phase 5 normalizes), identifier columns intentionally absent (§11 #1). Applied via `python -m db.init_db`; schema_version=2.
2. **Ingest module.** `db/sources/eff_atlas.py` — pure stdlib (`csv`, `urllib`, `sqlite3`, `re`). Mirrors MAC-3/MAC-4 shape: `--raw-subdir <ts>` reuse, single retry on `URLError`, delete-by-`source_id` then bulk-insert. CEO-ratified `source_type='crowdsourced'`, `source_url=https://atlasofsurveillance.org/download.csv` (dataset URL per row, NOT per-row Link 1), `citation_url` = per-row Link 1, deferred-decision marker in `extraction_runs.notes`. Header divergence is a stop-line (raises `ValueError`).
3. **§11 #3 PII redaction.** Regex `\b(rank-token)\s+[A-Z][a-z]+(?:\s+[A-Z][a-z]+)?` collapses to `[REDACTED-PERSON]` in `notes.summary_redacted` JSON. Raw Summary NEVER stored verbatim in DB. Full raw CSV preserved on disk: `sha256sum raw/eff_atlas/20260504T045109Z/atlas_of_surveillance.csv` = `45fc50f04e005e8ee1fb5c3500060d247221dfff82bea37f7e7e8fc4fa365933` (unchanged from manifest). Confirmed AOS001160 redacts "Mayor Gary Smith" → `[REDACTED-PERSON]` in DB; `grep -c "Gary Smith"` on raw CSV still returns 1.
4. **Staging counts.** sources.id=5 (`name='EFF Atlas of Surveillance (EFF + UNLV Reynolds School of Journalism)'`, `source_type='crowdsourced'`, `tier=1`); extraction_runs.id=9 (initial) + .id=10 (idempotency re-run) both `records_in=records_out=15,071`, `status='ok'`. `deployment_observations` for source_id=5: 15,071 rows. Notes JSON carries: `summary_redacted` (full redacted text), `redaction_hits`, `ori9` (NEWAOSNUMBER), `tech_abv`, `links` (3-element list of Link {1,2,3}+Snapshot+Source+Type+Date), `other_links`. `country='US'` set verbatim (Atlas covers US deployments only). `lat`/`lon` NULL — Atlas has no geo coords (DeFlock will be the geo source).
5. **Redaction log.** 16 hits across 16 rows, AOSNUMBERs `AOS001160, AOS002837, AOS003358, AOS003464, AOS003520, AOS006874, AOS006937, AOS008968, AOS008972, AOS010093, AOS014015, AOS014031, AOS014676, AOS014756, AOS015350, AOS015352`. Full list in `extraction_runs.notes`. The wider rank-token net (vs CEO's tight rank+first+last) is intentional per CP4 — recall over precision on PII at ingest, raw-on-disk recovery path for false-positive agency-name corruption, Phase 5 stricter pass.
6. **Idempotency verified.** Two consecutive `--raw-subdir 20260504T045109Z` runs both produced 15,071 rows (extraction_runs 9 + 10 identical counts, `status=ok`). The UNIQUE(source_id, source_row_key) backstop never fires because the delete-by-source-id pattern leaves a clean slate; backstop is a structural defence not a runtime path.
7. **No promotion to `identifiers`.** Phase 2 staging only per §11 #8. `identifiers` table remains 0 rows.
8. **25/25 dedup tests pass.** No regressions — migration is purely additive.
9. **Cop-car / Flock advisory sweep** (word-boundary regex per MAC-4 lesson):
   - **Flock Safety**: 2,743 deployments (2,707 single-vendor + 36 multi-vendor co-occurrences with Motorola Solutions / Vigilant Solutions / Axon / Genetec). Top states TX (229) / IL (228) / OH (222) / IN (212) / CA (187) — Midwest+South cluster, CA at #5. Tech split: ALPR 2,594 / Gunshot 45 / Video Analytics 34 / Camera Registry 16 / Drones 14 / RTCC 3 / Face Recognition 1 — Atlas treats Flock primarily as ALPR (correct).
   - **Motorola Solutions**: 301 mentions (217 single + 84 multi). Tech split skews **Camera Registry 117** > ALPR 56 > Body-worn Cameras 39 — interesting because Atlas annotators tag Motorola Vigilant ALPR deployments as "Camera Registry" more than "ALPR." Phase 5 categorization will need to dual-resolve.
   - **Axon**: 912 (893 single + 19 multi). Body-worn 827 dominates as expected; Camera Registry 45 is the second category — likely Axon Fleet 3 dashcam coverage.
   - **WatchGuard**: 181 — split across canonical 'WatchGuard'(162) + lowercase 'Watchguard'(14) + 'Motorola Solutions/WatchGuard'(2). Phase 5 canonicalization opportunity.
   - **Getac**: **17 deployments** — small but present. MAC-3 sweep had Getac absent from IEEE/Wireshark, flagged as Phase 3 FCC-ID. Atlas confirms Getac in active LE deployment (probably as in-vehicle laptop / dock ecosystem). Reinforces FCC-ID as the right Phase 3 path for Getac.
   - **Cradlepoint, Sierra Wireless, Cellebrite, Hak5, Magnet, Berla, Dedrone**: 0 mentions. Atlas captures *system-vendors* (Flock, Axon, Motorola) not *subsystem-vendors* (in-vehicle routers, forensics tools, drone-detection). Reaffirms Phase 3 (FCC-ID + SAM.gov) as the right path for these. The cop-car cluster sub-system half is structurally invisible to Atlas-style journalism.
   - **Multi-vendor co-occurrences** (raw, preserved): `'Skydio/Axon'(3)`, `'Flock Safety, Motorola Solutions'(10)`, `'Vigilant Solutions, Flock Safety'(5)`. Phase 5 cross-vendor join opportunity.
   - **Vendor canonicalization opportunities flagged for Phase 5**: 'WatchGuard'/'Watchguard', 'SoundThinking'/'Soundthinking', 'ShotSpotter'/'Shotspotter', 'BRINC'/'Brinc', 'Harris Corp.' legacy spelling.
10. **Reassigning MAC-5 `in_review` to CEO** with this summary. Phase 2 status: 3/4 Tier 1 sources landed (IEEE OUI ✓, Wireshark `manuf` ✓, EFF Atlas ✓); MAC-6 DeFlock pending dispatch.

CEO heartbeat 2026-05-04T~04:5xZ — MAC-5 ratified, Correction Pass 4 landed:
CEO heartbeat 2026-05-04T~04:5xZ — MAC-5 ratified, Correction Pass 4 landed:
1. **Verified all SourceWorker claims independently before ratifying** (per the verify-before-recap rule). On-disk: `sha256sum` matches manifest exactly (`45fc50f04e005e8ee1fb5c3500060d247221dfff82bea37f7e7e8fc4fa365933`); byte_count 8,361,059 matches; `python -c "csv.reader"` confirms 15,071 data rows + 28 columns; AOSNUMBER unique 15,071/15,071. Independent PII regex sweep (case-sensitive `\b(rank-token)\s+[A-Z][a-z]+`) reproduces 12 raw hits aligned with worker's 16 (worker's set slightly wider); the *real-PII* tightening (rank + first + last name pattern) yields exactly 1 hit confirming AOS001160 `Mayor Gary Smith`. License CC BY 4.0 verbatim from manifest.
2. **Ratified all 5 worker proposal items.**
   - Schema: **Option B** (new `deployment_observations` table) — schema honesty + Phase 3/5 query shape + MAC-6 absorbs cleanly + CP1 `procurement_records` precedent.
   - `source_type`: **`crowdsourced`** — Atlas describes own process as crowdsourcing/data journalism/public records; §8.2 50–75 band fits single-citation deployment claims; aligns with MAC-4 Wireshark `manuf` precedent.
   - PII handling: **redact rank+name in `notes` JSON, preserve full Summary in `raw/`, log redaction count in `extraction_runs.notes`** — honors §11 #3 (DB scrubbed) + §7.2 (audit trail preserved). Phase 5 promotion runs a stricter pass.
   - `source_url`: **dataset URL** (`https://atlasofsurveillance.org/download.csv`); Link 1 captured as separate `citation_url` column — Link 1 is Atlas's *external* journalistic citation, structurally distinct from "the source we are attributing to."
   - Bible amendment routing: **CEO logs as Correction Pass entry** (CP4) — confirmed; not a SAR.
3. **Bible Correction Pass 4 applied** in commit [`d81de3b`](`docs(bible): correction pass 4 — §4.2 deployment_observations supporting-table addition`). §4.2 gains `deployment_observations` bullet describing scope (deployment metadata, no identifiers, idempotency by `(source_id, source_row_key)`); `raw_observations` description tightened to clarify it stages identifier-bearing rows. CP4 entry in `BIBLE_AMENDMENTS.md` records: Option B rationale, draft DDL shape, the three bundled operational ratifications (source_type / PII handling / source_url) as ride-along context (not separate SARs), and the existing CP1-era `procurement_records` doc gap as an out-of-bible observation surfaced for user judgment but not corrected here per the strategic-steers-as-soft-priors rule. Hash backfilled in same file.
4. **Out-of-bible observation surfaced (no fix applied):** `procurement_records` (created in MAC-2 / Phase 1 per §4.5 carveout, signed off at Checkpoint 1) was never reflected in the §4.2 enumeration. This is a CP1-era documentation drift that predates MAC-5. Held for user judgment whether to roll a tidiness pass — not editing without explicit direction.
5. **MAC-5 PATCHed back to SourceWorker `in_progress`** with green-light comment quoting the 5 ratifications + bible HEAD `d81de3b` + reminder to use `--raw-subdir 20260504T045109Z` (no re-fetch). Worker authors `db/migrations/0002_deployment_observations.sql` to match the bible-at-HEAD shape, applies it, ingests, runs idempotency re-run, reports back `in_review` with: row count, vendor + technology + geographic + agency-cluster sweep counts (word-boundary regex per the MAC-4 lesson), §11 #3 redaction count, full Summary preservation confirmed on disk, manifest sha re-quoted from `manifest.json`.

SourceWorker heartbeat 2026-05-04T04:53Z — MAC-5 EFF Atlas schema proposal posted, awaiting CEO ratification:
1. **Source verified.** Canonical export: `https://atlasofsurveillance.org/download.csv` (HTTP 200, `text/csv`, content-disposition filename `Atlas of Surveillance-20260504.csv`). License: Creative Commons Attribution 4.0 International (CC BY 4.0) verbatim from https://www.eff.org/copyright. `robots.txt` empty — no `Disallow`. No documented rate limits in `/about` or `/methodology`. §11 #2 / #6 clear.
2. **Probe fetch + pre-stage.** Fetched once into `raw/eff_atlas/20260504T045109Z/atlas_of_surveillance.csv` (8,361,059 bytes; sha256 `45fc50f04e005e8ee1fb5c3500060d247221dfff82bea37f7e7e8fc4fa365933`) with co-located `manifest.json` (license string, attribution, content-disposition filename, ingest_status=`raw_only_pending_ceo_schema_ratification`). Will reuse via `--raw-subdir` once ratified.
3. **CSV shape.** 15,071 data rows; 28 cols (`AOSNUMBER, NEWAOSNUMBER (ORI9), City, County, State, Agency, Type of LEA, Summary, Type of Juris, Technology, TECH ABV, Vendor, Link 1..3 (+Snapshot/Source/Type/Date), Other Links`). **No lat/lon column** — Atlas geo is City/County/State only (dispatch description anticipated lat/lon "when present"; reality is "never present" — DeFlock will be the geo source). AOSNUMBER unique 15,071/15,071 → use as `source_row_key` natural key. Top-tech: Body-worn Cameras 5,465 / ALPR 4,084 / Drones 1,805 / Third-party Investigative 1,055 / Face Recognition 975. Top-vendor: Flock Safety 2,707 / Axon 892 / SoundThinking 796 / DJI 662 / Idemia 257 / Motorola Solutions 217. (Reported only for proposal context — not the post-ratification vendor sweep.)
4. **Schema proposal — Option B (new `deployment_observations` table).** Recommended over Option A because `raw_observations.candidate_identifier` is documented as a "pre-normalization identifier" from §4.1 enum; Atlas rows have no identifiers (§11 #1), and stuffing `eff_atlas:AOS000001` into that column is a category error that pollutes Phase 3/5 reads. Option B absorbs MAC-6 DeFlock (also a deployment source) without revisiting. Bible classification: §4.2 supporting-table addition → CEO logs as Correction Pass entry in `BIBLE_AMENDMENTS.md` (not SAR territory; §4.1 enums + main `identifiers` schema untouched). Migration would be `db/migrations/0002_deployment_observations.sql`. Draft DDL embedded in proposal comment.
5. **`source_type` proposal — `crowdsourced`.** Atlas's own about-page describes the process as "crowdsourcing, data journalism and public records reporting"; per-row lineage is volunteer + student researchers extracting from public reporting; Atlas curates but does not independently verify. §8.2 `crowdsourced` band 50–75 honestly reflects "single-citation deployment claim from public reporting." Aligns with MAC-4 Wireshark `manuf` precedent. `academic` flagged as defensible alternative — CEO call.
6. **§11 #3 PII surface.** Sweep on Summary field: 16 raw rank+name hits, **1 confirmed PII** (`AOS001160` — `"according to Mayor Gary Smith"`); 15 false positives are agency/org names containing rank-shaped tokens (Major Police and Fire Supply, Constable Precinct 5, Major Crash Assistance Team). 0 phone-number hits. 0 street-address hits. Proposed handling: redact rank+name → `[REDACTED-PERSON]` in `notes` JSON; preserve full Summary on disk in `raw/`; log redaction count in `extraction_runs.notes`; stricter pass at Phase 5 promotion. Surfaced for CEO ratification (alternatives: drop Summary entirely / keep verbatim).
7. **Posted proposal comment** ([1037c17e](/MAC/issues/MAC-5#comment-1037c17e-48f4-4ab9-9df7-466af011dbbe)); PATCHed MAC-5 to `in_review` and reassigned to CEO. **No DB writes anywhere** per dispatch's "do not write any rows until CEO ratifies." `db/argus.db` untouched; `db/migrations/0002_*.sql` not yet authored.

CEO heartbeat 2026-05-04T~04:42Z — MAC-4 closed `done`; MAC-5 EFF Atlas dispatched:
1. **Re-verified SourceWorker correction.** `grep -ic getac` on raw `manuf` = 0 ✓; `b4:1e:52` line `B4:1E:52  FlockSafety  Flock Safety` ✓; `8c:1f:64:57:40/36 FlockAudio  Flock Audio Inc.` ✓ as separate company. Substring-sweep correction (Axon: 13 raw → 1 canonical) reproduces. Ingest deliverables independently re-checked: sources.id=4 + extraction_runs 7+8 + raw_observations 57,009 (39,085 oui + 17,924 mac_range) + identifiers=0 all match comment exactly.
2. **Closed MAC-4 `done`** (PATCH with closing comment): reproduces verification, confirms staging stands, logs three lessons forward to MAC-5/6 (substring-sweep word-boundary rule, cross-source canonical-name agreement as a confidence signal, Getac/BRINC/Dedrone/DroneShield/Hak5/Vigilant/Magnet/Reveal/Rekor stay Phase 3 FCC-ID — none of them appeared in Wireshark either). Phase 2 status table: 2/4 Tier 1 sources landed.
3. **Dispatched MAC-5 EFF Atlas of Surveillance** as new issue [`5bb18739-d6cc-450d-8218-469cdf6559b7`](/MAC/issues/MAC-5) (parentId=MAC-1, goalId=`374f4ac8-…`, projectId=`7e80ffb6-…`, assigneeAgentId=SourceWorker, status=todo, priority=medium). Description spells out:
   - **§11 #1 explicit framing:** Atlas yields agency × technology × location × vendor metadata, **not** MAC/OUI identifiers. Identifier columns left NULL — no fabrication.
   - **Source discovery:** find canonical Atlas export URL (`atlasofsurveillance.org/data` entry point), record license (CC BY 4.0 expected, verify), respect EFF ToS.
   - **`source_type` choice:** discuss in dispatch comment (`academic` vs `crowdsourced`); stop-and-ask if neither fits, like MAC-4. (§11 #11.)
   - **Schema-fit decision deferred to SourceWorker proposal** with two options spelled out: (A) extend `raw_observations` with new `candidate_type='deployment_record'` + composite non-MAC `candidate_identifier`; (B) new staging table `deployment_observations` with Atlas-specific columns. Both options have draft DDL guidance + tradeoffs. SourceWorker writes a recommendation; CEO ratifies before any DB write. CHECK constraint = stop-line.
   - **Vendor + agency sweep spec** with explicit word-boundary / canonical-equality requirement (MAC-4 lesson codified).
   - **PII strip discipline** (§11 #3) — Atlas can include officer names; redact at staging time.
   - **No promotion to `identifiers`** (§11 #8) — Atlas can never directly promote anyway.
4. **Sequence reaffirmed:** MAC-5 → MAC-6 DeFlock → Checkpoint 2 to user. Single-worker sequential default.

SourceWorker heartbeat 2026-05-04T04:39Z — MAC-4 corrective follow-up posted; comment-only fix to vendor-sweep narrative, no code/data changes:
1. **CEO sent MAC-4 back `in_progress`** (comment [a3a663d9](/MAC/issues/MAC-4#comment-a3a663d9-7f3e-494a-850c-618483a10445)) flagging two factual errors in the vendor-sweep narrative I posted on MAC-4. Independent verification confirmed: ingest itself is clean (sha `f0af76eb…f994890a7`, byte_count 3,087,561, sources.id=4 + extraction_runs 7+8 + 57,009 raw_observations all match comment exactly; identifiers still 0).
2. **Errors:** (a) Getac claimed present in Wireshark `manuf` — actually absent (`grep -ic getac` = 0; stays Phase 3 FCC-ID per [MAC-3](/MAC/issues/MAC-3) scope). (b) Flock entry was backward — Wireshark **does** list `B4:1E:52 Flock Safety` canonically, identical `long_name` to IEEE; Flock Audio at `8c:1f:64:57:40/36` is a separate company, not a Flock-Safety re-attribution. The "IEEE↔Wireshark name divergence as confidence signal" framing does not apply on this OUI.
3. **Substring-sweep limitation flagged generally.** "Axon: 13 hits" was a raw substring count and included Axona / Interaxon / Maxon / Yaxon / Praxon / Paxonet / Axonne false positives. True canonical Axon Enterprise = 1 OUI (`00:25:DF`). Going forward, vendor-sweep narratives need word-boundary or canonical-name verification before reporting "present" — flagged to avoid recurrence on MAC-5 EFF Atlas + MAC-6 DeFlock.
4. **Posted corrective comment** ([f299b4a4](/MAC/issues/MAC-4#comment-f299b4a4-4b63-4cc6-ba65-84d3e84f1ba0)): strikes Getac claim, rewrites Flock entry, notes substring-sweep limitation, summarizes downstream impact. PATCHed MAC-4 to `in_review` and reassigned to CEO. No code or DB changes — staging stands.
5. **Phase 2 status unchanged:** 2/4 Tier 1 sources done (IEEE OUI ✓, Wireshark `manuf` ✓); MAC-5 EFF Atlas + MAC-6 DeFlock pending CEO dispatch after re-review of MAC-4.

SourceWorker heartbeat 2026-05-04T04:25Z — MAC-4 Wireshark `manuf` ingest landed:
1. **CEO decision applied (b0c99953).** Set `SOURCE_TYPE = 'crowdsourced'` in `db/sources/wireshark_manuf.py` (replacing the earlier `community` placeholder that triggered the `sources.source_type` CHECK violation). Updated module docstring with the §4.1 enum reasoning + Phase-5 reconsider note. No Bible amendment, no SAR, no enum extension — Wireshark `manuf` lands under existing `crowdsourced` (alongside WiGLE/DeFlock per §8.2).
2. **Resumed with pre-fetched manifest.** Re-ran `python -m db.sources.wireshark_manuf --raw-subdir 20260504T041753Z --agent-id <self>` — manifest hash verified (`f0af76eba2613fca9fda0d370da662e4bcf94d58a1903d1f273c7a5f994890a7`, byte_count=3,087,561), no network re-fetch.
3. **Staging populated.** `sources.id=4` (`source_type='crowdsourced'`, `tier=1`, `last_status='ok'`), `extraction_runs.id=7` (`records_in=records_out=57,009`, `errors=0`, `status='ok'`); notes carry the deferred-decision marker `"Wireshark manuf classified crowdsourced; reconsider at Phase 5 if confidence band under-fits."` so the Phase 5 reviewer sees it without grepping comments. `raw_observations` for `source_id=4`: 57,009 rows = 39,085 `oui` + 17,924 `mac_range`.
4. **Spot-check.** OUI sample: `00:00:00 → 'Officially Xerox, but 0:0:0:0:0:0 is more common'`, `00:00:01 → Xerox Corporation` (canonical IEEE root). mac_range sample: `00:1B:C5:00:00/36 → 00:1b:c5:00:0` (matches docstring spec, lowercase-colon-hex with trailing nibble). Manufacturer kept verbatim in `candidate_manufacturer`; raw line in `source_excerpt`; short_name + (if present) comment serialized into `notes` JSON.
5. **Idempotency verified.** Re-ran the ingest a second time (extraction_runs.id=8) — same 57,009 rows, no duplication; delete-by-`source_id` then bulk-insert pattern from `db/sources/ieee_oui.py` (MAC-3 template) works as designed.
6. **Identifier normalization mirrors MAC-3.** Lowercase hex, `:` separators every 2 hex digits, trailing nibble preserved as half-byte group. `/28` and `/36` Wireshark masks normalize to `:0` / `:00:0` style — identical shape to the IEEE MA-M / MA-S `c8:5c:e2:7` / `8c:1f:64:af:a` outputs. Phase 5 canonicalization gets a stable input shape across both sources. Worked examples documented in module docstring.
7. **No promotion to `identifiers`.** Phase 2 staging only per §11 #8. `identifiers` table remains empty.
8. **25/25 dedup tests pass.** No regressions.

Reassigning MAC-4 `in_review` to CEO with this summary in the comment thread. Phase 2 status: 2/4 Tier 1 sources landed (IEEE OUI ✓, Wireshark `manuf` ✓); EFF Atlas (MAC-5) and DeFlock (MAC-6) await CEO dispatch.

CEO heartbeat 2026-05-04T04:1xZ — MAC-3 review accepted, MAC-4 Wireshark `manuf` dispatched:
1. **MAC-3 verification.** Spot-checked SourceWorker deliverables against comment [`96c14aea`](/MAC/issues/MAC-3#comment-96c14aea-0c37-47b1-b8b2-d818661e9ada): all 4 raw artifact sha256s match (`ma_l.csv` `93d7b15d…`, `ma_l.txt` `f78942fd…`, `ma_m.csv` `1c8d5f29…`, `ma_s.csv` `818bfc42…`); `raw_observations` total = 52,727 with the documented (39,355 / 6,380 / 6,992) split; `sources` = 3 rows with byte_count + content_sha256 + http_status + license JSON-encoded in `notes`; `extraction_runs` = 6 (3 ingest + 3 idempotency re-runs, all `status=ok`); `identifiers` = 0 (no promotion, per §11 #8). Vendor-sweep counts re-confirmed (Flock 1, Cradlepoint 2, Sierra Wireless 6, Skydio 1, Axon Enterprise 1, Harris 5, Motorola family 152, ShotSpotter 1, Cellebrite 1, Avigilon Alta 1).
2. **Identifier normalization convention confirmed.** Lowercase hex with `:` separators every 2 hex digits, trailing nibble preserved as half-byte group (`28:6f:b9` / `c8:5c:e2:7` / `8c:1f:64:af:a`). Wireshark `manuf` ingest will mirror. This is the de-facto MAC-address representation; partial-byte half-byte groups are unusual but defensible because MA-M / MA-S are partial-byte ranges and Phase 5 does the final canonicalization anyway. No user prompt needed.
3. **§4.1 enum extension still deferred.** Phase 2 stages with `candidate_category=NULL` for Cradlepoint / Sierra Wireless rows, which is fine — the enum-extension question only bites at promotion time (Phase 3+). Standing item, not a blocker.
4. **MAC-3 closed `done`.** Comment ack acknowledging the worker's deliverables + answering the two open questions.
5. **MAC-4 dispatched.** Phase 2 source 2/4 — Wireshark `manuf` ingest. Same dispatch shape as MAC-3: pull bulk file, populate `sources` + `raw_observations` (one row per OUI / range entry), `source_type='community'` (Wireshark community-maintained), no canonicalization, no promotion. Idempotency via delete-by-source-id pattern from `db/sources/ieee_oui.py`. Stop-at-Checkpoint discipline; Checkpoint 2 fires only after all four Tier 1 sources land.

SourceWorker heartbeat 2026-05-04T04:0xZ — MAC-3 IEEE OUI ingest complete:
1. **Raw fetch.** Pulled all four IEEE registry artifacts to `raw/ieee_oui/20260504T040242Z/`: `ma_l.csv` (3.75 MB), `ma_l.txt` (6.45 MB; preserved for human-audit redundancy, not parsed), `ma_m.csv` (725 kB), `ma_s.csv` (647 kB). `manifest.json` co-located with URLs, byte counts, SHA-256 hashes, HTTP 200s. License note: public domain — IEEE OUI listing, freely redistributable.
2. **Ingest module.** Added `db/sources/ieee_oui.py` — pure-stdlib (`urllib.request`, `csv`, `sqlite3`). Single retry on transient `URLError`, no infinite retries (§7.2 Don'ts). Importable `ingest()` function + standalone CLI; supports `--raw-subdir <ts>` to reuse already-fetched files (used here for ingest-without-double-fetch, and for any future re-stage from existing raw artifacts).
3. **Idempotency choice.** Delete-by-`source_id` then bulk-insert under one transaction per registry block. Picked over a `UNIQUE(source_id, candidate_identifier)` constraint to avoid mutating the existing `raw_observations` schema; documented in module docstring. Re-running the ingest produces identical row counts (verified — 52,727 → 52,727).
4. **Identifier normalization.** Lowercase hex, `:` separators every 2 hex digits, trailing nibble preserved as half-byte group. Examples: `286FB9` → `28:6f:b9` (MA-L), `C85CE27` → `c8:5c:e2:7` (MA-M), `8C1F64AFA` → `8c:1f:64:af:a` (MA-S). Documented in module docstring; downstream Phase 5 normalization has a stable input shape.
5. **Type mapping.** MA-L → `candidate_type='oui'` (mirrors §4.1 enum); MA-M / MA-S → `candidate_type='mac_range'` (sub-OUI ranges).
6. **Staging counts.** `sources` table: 3 rows (one per block, MA-L txt is raw-only). `extraction_runs`: 6 (3 ingest runs + 3 idempotency-verification re-runs). `raw_observations`: 39,355 + 6,380 + 6,992 = 52,727. Manufacturer raw name kept verbatim in `candidate_manufacturer`; address kept verbatim in `notes` for later vendor-cluster work; first 200 chars of raw CSV row in `source_excerpt` for validator audit.
7. **Notable-vendor flags surfaced** in MAC-3 comment back to CEO (Flock Safety, Cradlepoint, Sierra Wireless, Motorola family, Axon, Skydio, Parrot, DJI, WatchGuard, Cellebrite, ShotSpotter, Avigilon, Genetec, Harris, Axis Communications all confirmed present; Getac, BRINC, Dedrone, DroneShield, Hak5, Vigilant Solutions, Magnet Forensics, Reveal, Rekor confirmed *absent* by canonical name — likely Phase 3 FCC-ID / Wireshark `manuf` complementary coverage).

CEO heartbeat 2026-05-04T03:2xZ — Correction Pass 3 + Phase 2 dispatch prep:
1. **Bible edit (§2.1 + §12).** Added §2.1 #5 "In-vehicle LTE/WiFi routers — Cradlepoint (IBR900/R1900-class), Sierra Wireless (MG90/AirLink-class)" between old #4 (police radios) and old #5 (drones). Renumbered old #5–#11 → #6–#12. Chose option (b) over option (a): a separate row keeps each §2.1 item one emitter class with consistent radio physics — LTE+WiFi backhaul is genuinely distinct from P25/VHF voice. User explicitly noted "in-vehicle routers aren't radios"; option (b) honors that. Also added `device_cluster_id` design question to §12 Open per user direction (initial lean: scanner-side correlation belongs in Talos, but holding for explicit decision).
2. **Manufacturers seed update (32 → 34).** Added Cradlepoint and Sierra Wireless rows in the police_radio→drone gap (matching new §2.1 #5 position). Both rows use `primary_category=NULL` (mirrors Motorola Solutions / L3Harris multi-purpose pattern) because the §4.1 device_category enum does not yet have an `in_vehicle_router` value — surfacing as a future enum-extension decision when Phase 2/3 actually ingests the first router identifier. Renumbered §2.1 # references throughout the seed comments (#5→#6 drones, #6→#7 gunshot, #7→#8 hacking, #10→#11 face_recog, #11→#12 drone_detect; Rekor's `#1, #10` → `#1, #11`). 25/25 dedup tests pass; argus_cli status clean; verified 34-vendor count + Cradlepoint/Sierra Wireless rows present.
3. **BIBLE_AMENDMENTS Correction Pass 3 entry** logged. Bible commit hash backfilled.
4. **Phase 2 hire request** queued via `paperclip-create-agent` skill — Source Worker for Tier 1 structured sources (IEEE OUI registry, Wireshark manuf, EFF Atlas of Surveillance, DeFlock). Brief includes the standing Flock + cop-cars advisory (high-value targets — surface anything interesting; not a scope narrowing).

## Research leads (Phase 3/4 breadcrumbs)

These are leads to follow — not commitments, not active work. Captured per user direction (MAC-1 comment [d08ee4a8](/MAC/issues/MAC-1#comment-d08ee4a8-e525-4c19-9886-89f0f95bf1c3)) so the breadcrumbs survive context drift between phases. Source Workers/Extraction Workers should pick these up at the relevant phase and report back with findings.

- **Flock SSID pattern (Phase 3/4).** Flock cameras likely emit a maintenance/installer WiFi network with an SSID convention along the lines of `Flock-{serial}` or similar (per public installer materials). Confirming the exact pattern from Flock FCC filings (grantee 2AKWH-class — needs verification) and installer documentation would yield a Tier-0 SSID-pattern record the Pi scanner can act on at high confidence even before any concrete MAC/OUI lands. Hold the actual scraping for Phase 3/4 (FCC ID workflow + manufacturer doc mining) — capture the lead now so it doesn't get lost.
- **FCC grantee-prefix harvesting (Phase 3 — alongside FCC ID workflow).** Identify FCC grantee codes for Flock, Motorola Solutions (APX, V300/V500), Axon (Body 2/3/4, Fleet 3), Cradlepoint (IBR/R-series), Sierra Wireless (AirLink/MG90), L3Harris (XL-series), DJI (Matrice LE), Skydio. Bulk-lookup §7.2 sources by grantee code rather than per-product searches; each grantee filing typically yields multiple OUIs + frequency ranges + internal photos in one pass. Higher yield per query than vendor-name searches.
- **City council minutes + municipal procurement portals (Phase 3/4 — alongside SAM.gov).** SAM.gov covers federal-side procurement, but Flock + body-cam + LPR deployments at municipal level often surface in city council minutes, police-commission meeting agendas, and small-city procurement portals. These are higher-yield than vendor websites for the "where is this deployed" mapping (which then feeds DeFlock + Atlas of Surveillance geo-priors). Worth a dedicated Source Worker workstream in Phase 3 or Phase 4 unstructured wave.
- **Cop-car emitter cluster (standing context for Source Workers, not a research lead per se).** A patrol car is a multi-emitter cluster: Motorola APX (P25 radio), Axon Fleet 3 dashcam (WiFi+BLE), Getac laptop (WiFi+BT), Cradlepoint/Sierra Wireless in-car router (LTE+WiFi), body cam BLE (Axon/Reveal/WatchGuard), driver phone. When any one of these is found in a deployment, the others are usually present too — useful for cross-corroboration during validation, and an argument for the §12 `device_cluster_id` question above.

## Open questions for human
- **§4.1 device_category enum extension** — Cradlepoint/Sierra Wireless are seeded with `primary_category=NULL` (multi-purpose pattern) because the enum doesn't yet have an `in_vehicle_router` value. _Blocking? n; flag at Phase 2 dispatch when the first router OUI/MAC actually lands. Future Correction Pass material._
- **§12 `device_cluster_id` design call** — added by Correction Pass 3. User holding for explicit decision; initial lean is scanner-side. Surface at Checkpoint 5 if unresolved by then. _Blocking? n_
- **WiGLE API credentials before Checkpoint 2** — user said "in your hands before you hit Checkpoint 2." User reconfirmed in MAC-1 d08ee4a8. _Blocking? n; required before Phase 3 Step-0 estimate fires._
- **Final project name confirmation at Checkpoint 5.** _Blocking? n_
- **`argus_record_id` upsert semantics in Talos v0.2 seeder** (BIBLE_AMENDMENTS SAR-2). _Blocking? n_

## Recent log (last 25 entries)
- 2026-05-04T02:30Z      CEO created Phase-1 subtask MAC-2 (`3e8c4825`) assigned to DBArchitect.
- 2026-05-04T02:35–02:37Z DBArchitect built migrations + init_db + manufacturers seed (32 vendors) + procurement_records + dedup module + 25 unit tests + argus_cli.py.
- 2026-05-04T02:42Z      DBArchitect returned MAC-2 `in_review` to CEO with original Checkpoint 1 summary (comment `765d6d31`).
- 2026-05-04T02:46Z      CEO verified artifacts; posted Checkpoint 1 review (comment `caba4a10`); reassigned MAC-2 to user `in_review`.
- 2026-05-04T02:52:55Z  Wake — MAC-1 comment 5d75988d: corrections approved BUT Phase-1 approval rescinded; require BIBLE_AMENDMENTS.md + re-issued Checkpoint 0.
- 2026-05-04T02:55Z      CEO blocked MAC-2 on MAC-1 (status=blocked, reassigned to DBArchitect).
- 2026-05-04T02:56Z      CEO wrote `BIBLE_AMENDMENTS.md`; commit `ab2e45c` (Correction Pass 1 + SAR-1 + SAR-2).
- 2026-05-04T02:57Z      CEO posted re-issued Checkpoint 0 status on MAC-1 (comment `4a7eb700`); state committed `9dc3cd9`.
- 2026-05-04T03:00:37Z  Wake — MAC-1 comment f08cd82b: ✅ Checkpoint 0 re-signed-off, ✅ Phase 1 re-approved, "re-validate don't rebuild," fill §11 #11 with the BIBLE_AMENDMENTS discipline rule, log Correction Pass 2, add SAR preamble note.
- 2026-05-04T03:0xZ      CEO applied §11 #11 fill (`1cfbbd4`), backfilled hash (`8590292`), added SAR preamble note. Reassigned MAC-2 to DBArchitect `todo` with re-validation spec; state committed `95bdd53`.
- 2026-05-04T03:07:33Z  Wake — MAC-1 comment 404742ab: user narrowed V1 focus to **Flock cameras + cop cars**. CEO surfaced bible gap (no Cradlepoint / Sierra Wireless in §2.1) and asked scope-vs-priority.
- 2026-05-04T03:11:25Z  Wake — MAC-2 comment ca3f4b75: DBArchitect Checkpoint 1 re-validation **complete, no deltas**. Schema/dedup/CLI/seed all match HEAD `8590292`.
- 2026-05-04T03:13:41Z  Wake — MAC-1 comment 13461971: user clarified Flock + cop cars is a soft prior, NOT scope narrowing or priority reweighting. No SAR-3, no Correction Pass 3 from this steer alone.
- 2026-05-04T03:23:31Z  Wake — MAC-1 comment d08ee4a8: 🟢 **Checkpoint 1 signed off.** Cradlepoint/Sierra Wireless gap approved as Correction Pass 3 (independent of the Flock advisory). device_cluster_id → §12. Research leads → PROJECT_STATE. Phase 2 hire request next, brief Source Workers with the standing Flock + cop-cars advisory.
- 2026-05-04T03:2xZ      CEO this heartbeat: applied Correction Pass 3 (§2.1 #5 added — option b; renumbered #5–#11 → #6–#12); added device_cluster_id question to §12 Open; updated manufacturers seed (32 → 34) with Cradlepoint + Sierra Wireless; renumbered §2.1 # references in seed comments; rebuilt argus.db; 25/25 dedup tests pass; argus_cli status clean; logged Correction Pass 3 in BIBLE_AMENDMENTS; added research leads section above. Closing MAC-2 done. Phase 2 Source Worker hire request next.
- 2026-05-04T03:33:43Z   CEO submitted SourceWorker hire request (approval `9bd70f48`, agent `9cf8ff12-...`); AGENTS.md staged with Phase 2 Tier 1 scope + Bible §7.2 + standing Flock+cop-cars advisory.
- 2026-05-04T04:00:01Z   User approved SourceWorker hire (`9bd70f48`). Agent transitioned `pending_approval → idle`.
- 2026-05-04T04:01:26Z   CEO dispatched MAC-3 IEEE OUI registry ingest as Phase 2 source 1/4 to SourceWorker. parentId=MAC-1, goalId=`374f4ac8-...`, status=todo. SourceWorker will pull MA-L/MA-M/MA-S registry CSVs into `raw/ieee_oui/<ts>/`, populate `sources` + `raw_observations`, and report back `in_review` to CEO with vendor-cluster flags.
- 2026-05-04T04:0xZ       SourceWorker (this heartbeat) ran MAC-3 IEEE OUI ingest. Fetched all four IEEE artifacts into `raw/ieee_oui/20260504T040242Z/` with manifest+SHA-256s. Built `db/sources/ieee_oui.py` (stdlib-only ingest with one-retry HTTP, delete-by-source-id idempotency, lowercase-colon-hex normalization). Staged 39,355 (MA-L `oui`) + 6,380 (MA-M `mac_range`) + 6,992 (MA-S `mac_range`) = **52,727** rows in `raw_observations` + 3 rows in `sources`. Idempotency verified by re-run (count stable). Notable-vendor sweep posted in MAC-3 comment to CEO: Flock Safety `b4:1e:52` confirmed; Cradlepoint 2 OUIs Boise; Sierra Wireless 6 OUIs Richmond BC; Skydio 1 OUI; Motorola family 152 blocks (broad, not OUI-categorizable per §8.4); Axon Enterprise present alongside name-collision noise to resolve in Phase 5; Harris Corp Melbourne FL confirmed; ShotSpotter+Cellebrite+WatchGuard Video confirmed. **Absent from IEEE by canonical name** (route to Phase 3 FCC-ID + Wireshark `manuf` complementary coverage): Getac, BRINC, Dedrone, DroneShield, Hak5, Vigilant Solutions, Magnet Forensics, Reveal, Rekor. Reassigned MAC-3 `in_review` to CEO; awaiting Wireshark `manuf` dispatch (Phase 2 source 2/4).
- 2026-05-04T04:1xZ       CEO verified MAC-3 deliverables (sha256 file match × 4; DB row counts match comment exactly; idempotency re-run stable; 0 identifiers per §11 #8). Confirmed identifier normalization convention (`28:6f:b9` style) — Wireshark to mirror. Closed MAC-3 `done`. Dispatched MAC-4 Wireshark `manuf` ingest as Phase 2 source 2/4 to SourceWorker; same shape as MAC-3, idempotency via existing `db/sources/ieee_oui.py` template.
- 2026-05-04T04:17:53Z    SourceWorker fetched Wireshark `manuf` (3,087,561 bytes; sha256 `f0af76eb…890a7`; HTTP 200) into `raw/wireshark_manuf/20260504T041753Z/` with manifest. Built `db/sources/wireshark_manuf.py` (stdlib-only, /28+/36 mask normalization, delete-by-source-id idempotency). First insert attempt blocked: dispatch said `source_type='community'` but §4.1 enum / migration `0001_initial.sql` CHECK only accepts {`official`,`regulatory`,`procurement`,`academic`,`foia`,`crowdsourced`,`inferred`,`manufacturer_doc`}. Stopped per §11 #11 (no silent coercion); reassigned MAC-4 `blocked` to CEO with three options.
- 2026-05-04T04:23:00Z    Wake — MAC-4 comment b0c99953: CEO chose option (a) `crowdsourced` (matches §8.2 WiGLE/DeFlock band 50–75); no Bible amendment, no SAR, no Correction Pass. Resume with `--raw-subdir 20260504T041753Z`; record one-line deferred-decision marker in `extraction_runs.notes`.
- 2026-05-04T04:25:09Z    SourceWorker (this heartbeat) applied CEO decision: `SOURCE_TYPE = 'crowdsourced'` in `db/sources/wireshark_manuf.py`, docstring updated, deferred-decision marker added to `_finish_run` notes. Re-ran with pre-fetched manifest (no network re-fetch) — manifest hash verified. Staged **57,009 rows** (39,085 `oui` + 17,924 `mac_range`) under `sources.id=4` / `extraction_runs.id=7`. Idempotency re-confirmed (run 8, same count). 25/25 dedup tests pass. `identifiers` table still empty (§11 #8). Phase 2 status: 2/4 Tier 1 sources done (IEEE OUI ✓, Wireshark `manuf` ✓). Reassigning MAC-4 `in_review` to CEO.
- 2026-05-04T04:34:14Z    Wake — MAC-4 comment [a3a663d9](/MAC/issues/MAC-4#comment-a3a663d9-7f3e-494a-850c-618483a10445): CEO independently verified the ingest is clean but flagged two factual errors in the vendor-sweep narrative (Getac claimed present — actually absent; Flock claimed name-divergent at `b4:1e:52` — actually canonical match with IEEE). Comment-only fix requested, no code/data changes.
- 2026-05-04T04:39:22Z    SourceWorker (this heartbeat) re-grepped raw `manuf` (sha re-confirmed); CEO findings reproduce. Posted corrective comment [f299b4a4](/MAC/issues/MAC-4#comment-f299b4a4-4b63-4cc6-ba65-84d3e84f1ba0): strikes Getac claim, rewrites Flock entry, notes substring-sweep limitation (true canonical Axon Enterprise = 1 OUI, not 13). PATCHed MAC-4 to `in_review` reassigned to CEO. Staging untouched (sources.id=4, raw_observations 57,009, identifiers 0).
- 2026-05-04T~04:42Z       CEO this heartbeat: re-verified worker's correction (sha + grep + canonical-line spot-checks reproduce). Closed [MAC-4](/MAC/issues/MAC-4) `done` with closing comment (lessons forward: word-boundary vendor sweep, cross-source canonical-name agreement = corroboration signal, Getac + 8 other IEEE-absent vendors stay Phase 3 FCC-ID per re-confirmed Wireshark sweep). Dispatched [MAC-5](/MAC/issues/MAC-5) EFF Atlas of Surveillance ingest as new issue `5bb18739-…` to SourceWorker (`todo`, parentId=MAC-1). Description: explicit §11 #1 framing (no identifier fabrication — Atlas is metadata only); schema-fit options A/B with stop-at-CHECK discipline; vendor sweep word-boundary requirement; §11 #3 PII strip; §11 #6 EFF ToS respect; `source_type` choice (`academic` vs `crowdsourced`) deferred to worker proposal. MAC-1 stays `in_review` with user.

## Notes for human review
- **Cradlepoint/Sierra Wireless seeded with `primary_category=NULL`.** The §4.1 device_category enum does not yet have an `in_vehicle_router` value. Mirrors the Motorola Solutions / L3Harris multi-purpose-vendor pattern. The enum extension is a deliberate future decision: surface at Phase 2 dispatch if/when the first router OUI/MAC actually needs a category, not a Correction Pass 3 deliverable.
- **§2.1 number renumbering cascade was bounded.** Bible §2.1, seed comments in `db/migrations/0001_initial.sql`. PROJECT_STATE.md line-13 (in the historical log) references `#8/#9` — left as-is because that entry describes the state at the time it was written (32-vendor seed, pre-renumber) and is historical record, not live cross-reference. Going forward, all §2.1 # refs in new entries use the post-Correction-Pass-3 numbering.
